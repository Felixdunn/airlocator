# Vercel Configuration
# Removed cron jobs - scanning is now manual/user-triggered

{
  "framework": "nextjs",
  "buildCommand": "next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "env": {
    "PLATFORM_FEE_BPS": "200"
  }
}
